This project contains Ruby written package RPM specs that can be built against Ruby 1.9 and CentOS

### How to build

# yum install -y rpm-build rpmdevtools readline-devel ncurses-devel gdbm-devel tcl-devel openssl-devel db4-devel byacc gcc-c++ augeas-devel
# make
# The output RPMs will be put at ~/rpmbuild/RPMS

